syntax = "proto3";

package arcanum.mana.v1;

option go_package = "arcanum/x/mana/types";

// Params defines the configurable parameters for the mana module.
message Params {
  string admin = 1;
  bool transfer_enabled = 2;
}

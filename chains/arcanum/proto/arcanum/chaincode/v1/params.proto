syntax = "proto3";

package arcanum.chaincode.v1;

option go_package = "arcanum/x/chaincode/types";

// Params defines the configurable parameters for the chaincode module.
message Params {
  string admin = 1;
  bool non_transferable = 2;
}

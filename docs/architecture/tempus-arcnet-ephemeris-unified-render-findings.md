---
title: "Tempus × ARCnet Ephemeris Unified Render Findings"
status: design-candidate
visibility: public
last_updated: 2026-08-23
phase: Pre-Genesis
authority: non-canonical render and falsification evidence
---

# Purpose

Record the findings produced while rendering the ephemeris-resolved Tempus dodecahedral compass and transported ARCnet stellar frame as one 3D system.

Primary artifacts:

- `docs/architecture/registries/tempus-ephemeris-arcnet-unification-registry.v0.1.json`
- `docs/architecture/tempus-ephemeris-arcnet-unification-audit.md`
- `docs/architecture/renderings/tempus-arcnet-ephemeris-unified-3d-v0.1.html`

# 1. The dual scaffold removes an arbitrary nesting decision

The outer twelve-face regular dodecahedron already had a bounded-pass meaning as the Tempus zodiac/celestial shell.

Its twelve face centers are exactly the vertices of its regular icosahedral dual.

Therefore the inner icosahedron does not need to be assigned by count matching.

It is generated by the outer geometry itself.

This gives the first defensible nesting:

```text
Tempus dodecahedral shell
        ↓ exact face-center dual
Tempus inner icosahedral carrier scaffold
        ↓ registered zodiac Hamiltonian cycle
72-sector chronological carrier path
        ↓ ephemeris position
transported ARCnet stellar frame
```

Candidate law:

> **Derived Geometry Before Assigned Geometry — when an inner form is exactly generated by an outer form, preserve the derivation before searching for additional symbolic identities.**

# 2. Every zodiac region has equal carrier-path length

The registered chronological cycle uses twelve edges of the regular inner icosahedron.

For each sign:

```text
entry shared-edge midpoint → sign face center
+
sign face center → exit shared-edge midpoint
```

is exactly:

```text
1/2 regular icosahedron edge
+
1/2 regular icosahedron edge
=
1 regular icosahedron edge length.
```

Therefore all twelve signs receive equal carrier-path length in this renderer.

This equality is geometric.

It does not assert equal civil duration because the apparent solar longitude rate is nonuniform through the year.

Candidate law:

> **Equal Geometric Span Does Not Imply Equal Civil Duration.**

# 3. The seventy-two sectors become equal path intervals

Each thirty-degree sign contains six five-degree quinaries.

Because each sign receives one equal total carrier-path edge length, its six quinaries may be rendered as six equal carrier-phase intervals.

This yields:

```text
12 equal sign path spans
× 6 equal quinary phases
= 72 equal carrier-phase intervals
```

This is a property of the normalized projection.

The ephemeris determines how long the ARCnet frame spends in each interval in civil time.

# 4. The face center becomes a real phase landmark

The path reaches the dual vertex / dodecahedral face center at local sign phase `u = 0.5`, corresponding to 15° within the sign.

Therefore:

```text
Q1 + Q2 + first half of Q3
    → inward approach
15° face-center crossing
second half of Q4 + Q5 + Q6
    → outward departure
```

At the registered decan level:

```text
Decan 1 = Q1 + Q2
Decan 2 = Q3 + Q4
Decan 3 = Q5 + Q6
```

so Decan 2 is the center-crossing decan.

This is a renderer property only. It is not asserted as an ancient decan doctrine.

# 5. Sign boundaries become shared geometric points

The exit point of sign `i` is:

```text
(C_i + C_(i+1)) / 2
```

and the entry point of sign `i+1` is the same midpoint.

Therefore there is no teleport or reset when the ephemeris crosses 30°, 60°, 90°, and so on.

The ARCnet frame crosses a shared boundary continuously.

Candidate law:

> **Temporal Boundary May Change Label Without Breaking Trajectory.**

# 6. Ephemeris time and normalized time remain distinguishable

The current research snapshot at `2026-08-23T23:53:00Z` returned solar longitude approximately:

```text
150.8660349083°
```

which resolves to:

```text
Virgo
sector 31 / 72
quinary 1 / 6
decan 1 / 3
```

The instantaneous longitude speed was approximately:

```text
0.9637576554° / day
```

so a five-degree angular sector is not intrinsically a five-civil-day interval.

The render therefore treats:

```text
5° quinary
```

and:

```text
5-day contemplative pentad
```

as independent overlays that happen to share a seventy-two-count indexing possibility.

# 7. ARCnet survives transport without semantic deformation

The renderer verifies that moving the frame through the Tempus carrier changes only:

- global position;
- optional presentation orientation;
- current Tempus face/quinary/decan labels.

It does not change:

- the eight local ARCnet system coordinates;
- generative/stewardship tetrahedra;
- mirror pairs;
- inner derived octahedral junction construction;
- Edge Contract requirements;
- Identity center status;
- capability or authority.

This visually reinforces:

> **Transport the Frame; Do Not Rewrite the Frame.**

and:

> **The Sky May Position the Vehicle; It May Not Drive Its Authority.**

# 8. The inner icosahedron remains Tempus-local

The render makes a possible future confusion more likely because Vitae also has a separate design-candidate icosahedral corpus atlas.

The two structures must not be silently merged.

Current distinction:

```text
Tempus inner icosahedron
= 12 dual vertices generated from 12 dodecahedral face centers
= transport/navigation scaffold

Vitae icosahedron
= 20 faces used as a corpus atlas for 10 Grades + 10 Specializations
= curriculum/navigation projection
```

They share a Platonic solid.

They do not currently share a registry or semantic coordinate.

Candidate law:

> **A Shared Solid Does Not Merge Coordinate Systems.**

# 9. Render disposition

**PASS as a research visualization of the bounded unification contract.**

The render successfully shows:

```text
astronomical longitude
→ neutral Tempus coordinate
→ exact dual carrier geometry
→ rigid ARCnet transport
```

while retaining geometry-free equivalence and the authority firewall.

Human review is still required to determine whether the visual hierarchy communicates the distinction among:

- outer Tempus shell;
- inner dual carrier;
- current factual solar coordinate;
- transported ARCnet vehicle;
- non-geometric capability state.

# Next visual gate

The most informative next addition is an independent lunar ephemeris marker rendered on or around the same Tempus shell.

It must move independently from the solar carrier and must not alter ARCnet position unless the participant explicitly switches the selected Tempus coordinate from solar to lunar.

That test will determine whether the architecture can support multiple simultaneous factual cycles without collapsing them into one master celestial state.

# Mobile Termux Verification

Branch: mobile

Verified from Android Termux:

- pnpm install --ignore-scripts
- pnpm -C apps/web typecheck
- pnpm -C apps/web build

Initial mobile edit verified:
- Nexus module navigation now uses shared labeled module tab rail.
